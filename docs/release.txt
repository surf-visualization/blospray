0.1

scene management on the server
- improved exception handling on server (mostly json parsing)
- clear state in case of errors? or simply manually restart?

set basic color of geometry in UI (blender mesh, geometry, isosurfaces)

query and set bbox geometry from UI


0.2

allow plugins to set outline mesh geometry, not just bbox

allow setting slice planes using parenting

ospray 2.0


major features for later
------------------------

interactive preview render

material and tf editing

mpi-based parallel rendering

animation support